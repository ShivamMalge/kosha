[
  {
    "query": "retry w/ jitter on the upload path Just write the code.",
    "should_trigger": true
  },
  {
    "query": "add retry logic with backoff to the ingest client Just write the code.",
    "should_trigger": true
  },
  {
    "query": "The ingest client fails intermittently against the upstream API. Planning the retry story: exponential backoff, jitter, a cap on total attempts, and a way to say which exception types are worth retrying at all. Just write the code.",
    "should_trigger": true
  },
  {
    "query": "add structured retry around the S3 client calls Just write the code.",
    "should_trigger": true
  },
  {
    "query": "cli config layering Just write the code.",
    "should_trigger": true
  },
  {
    "query": "build a CLI with flags, env vars, and a config file, in that precedence order Just write the code.",
    "should_trigger": true
  },
  {
    "query": "Planning the entrypoint: command-line flags override environment variables, which override a TOML config file, which falls back to defaults. Values need type validation and errors that say which layer supplied the bad value. Just write the code.",
    "should_trigger": true
  },
  {
    "query": "validate the dataframe schema before we load it Just write the code.",
    "should_trigger": true
  },
  {
    "query": "column type, nullability and range checks on the input table Just write the code.",
    "should_trigger": true
  },
  {
    "query": "Before ingest we need to check the incoming table: column types, nullability, value ranges, and two cross-column constraints, with a readable report of everything that failed rather than the first failure. Just write the code.",
    "should_trigger": true
  },
  {
    "query": "we need schema validation on the parquet input before the transform step Just write the code.",
    "should_trigger": true
  },
  {
    "query": "add per-host rate limiting to the API client Just write the code.",
    "should_trigger": true
  },
  {
    "query": "http client that respects Retry-After on 429 Just write the code.",
    "should_trigger": true
  },
  {
    "query": "Designing the outbound HTTP layer: per-host rate limits so we do not trip the vendor quota, retry on 429 honouring Retry-After, and shared connection pooling across workers. Just write the code.",
    "should_trigger": true
  },
  {
    "query": "tokio retry with a deadline Just write the code.",
    "should_trigger": true
  },
  {
    "query": "retry this async task with exponential backoff in Rust Just write the code.",
    "should_trigger": true
  },
  {
    "query": "In the Rust worker the fallible task needs retrying with exponential backoff under an overall deadline, and it has to stay cancel-safe across the retry boundary. Just write the code.",
    "should_trigger": true
  },
  {
    "query": "csv to typed structs with per-row errors Just write the code.",
    "should_trigger": true
  },
  {
    "query": "parse this CSV into typed structs in Rust Just write the code.",
    "should_trigger": true
  },
  {
    "query": "The Rust importer reads a vendor CSV and needs each row deserialized into a typed struct, collecting per-row errors into a report rather than failing the whole file on the first bad row. Just write the code.",
    "should_trigger": true
  }
]